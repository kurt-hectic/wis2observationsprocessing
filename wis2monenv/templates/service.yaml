apiVersion: v1
kind: Service
metadata:
  name: {{ include "wis2monenv.fullname" . }}
  labels:
    {{- include "wis2monenv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wis2monenv.selectorLabels" . | nindent 4 }}
