apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wis2monenv.fullname" . }}-test-connection"
  labels:
    {{- include "wis2monenv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wis2monenv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
